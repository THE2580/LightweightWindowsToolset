请先完整阅读 `E:\codex_agent_project\LightweightWindowsToolset\PROJECT_SUMMARY.md` 理解项目全貌和当前进展，然后等待我的指令。

## 注意事项
请严格遵守以下规则：

- 代理: 所有网络操作必须通过 socks5://127.0.0.1:7897
- 构建: cd electron-app && npx electron-vite build（TypeScript strict: true）
- 开发: cd electron-app && npm run dev
- 打包: cd electron-app && npm run package（仅明确说"打包"时执行，打包前清理 dist 并 kill 轻量化工具集.exe 进程，CSC_IDENTITY_AUTO_DISCOVERY=false）
- 窗口: 676x444 固定不可调，侧边栏 155px/44px
- 主题: 蓝白灰（light #2563EB / dark #3B82F6），禁止渐变球/光晕装饰、禁止负 letter-spacing、禁止 UI 卡片嵌套卡片
- 窗口关闭: 默认直接退出，可选缩小到托盘；托盘右键"退出"必须终止进程
- AI 聊天: 内置功能非工具，不在工具列表和开关中
- 中文字符串: 必须 UTF-8 存储，严禁 PowerShell 管道传中文给 Python
- 已删除文件不要恢复（如 tavily.ts），已移除功能不要重新实现（如联网搜索、测试模块）
- 设置页: 编辑时显示保存按钮，保存成功后隐藏（不允许自动保存）
- 工具禁用 = 快捷键禁用: 通过 tool:set-enabled IPC 通知主进程
- 新工具默认无快捷键（默认空字符串）
- 快捷键: 追加式录入，保存时一次性检测，JSON 数组存储。规则：≥2 键、左修饰右普通、仅一个普通键。录入时禁用全局快捷键，保存/取消后恢复
- 原子化 commit: 每完成一个可独立运行的子功能提交，中文 message（feat:/fix:/chore:/style:/docs: 格式）
- 提交前验证: 代理可用 + npx electron-vite build 编译通过
- 每次修改后先自行编译验证再让用户查看
- 请全程使用中文回复我
- 完整阅读 PROJECT_SUMMARY.md 了解项目全貌和当前进展后，请先询问 "接下来干什么？"，等待用户指令，不要直接开始修改代码
